<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN"      
 "http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">
<mapper namespace="com.ud.cloudnote.dao.BookDao">
<cache type="com.ud.cloudnote.cache.MybatisRedisCache" />
<insert id="save" 
parameterType="com.ud.cloudnote.entity.Book">
insert into cn_notebook
	(cn_notebook_id,cn_user_id,
	cn_notebook_type_id,cn_notebook_name,
	cn_notebook_createtime)
values (#{cn_notebook_id},#{cn_user_id},
	'5',#{cn_notebook_name},
	#{cn_notebook_createtime})
</insert>

<select id="findByUserId" 
	parameterType="string"
	resultType="com.ud.cloudnote.entity.Book">
	select * from cn_notebook
	where cn_user_id=#{userId} and cn_notebook_name!="favorites"
</select>
	
<select id="findByName" parameterType="map" resultType="com.ud.cloudnote.entity.Book">
	select * from cn_notebook where cn_notebook_name=#{bookName} and cn_user_id=#{userId}
</select>

<select id="getFavId" parameterType="map" resultType="string">
	select cn_notebook_id from cn_notebook where cn_notebook_name=#{bookName} and cn_user_id=#{userId}
</select>

<update id="updateBookName" 
	parameterType="com.ud.cloudnote.entity.Book">
	update cn_notebook
	set cn_notebook_name=#{cn_notebook_name},
		cn_notebook_createtime=#{cn_notebook_createtime}
	where cn_notebook_id=#{cn_notebook_id} and cn_user_id=#{cn_user_id}
</update>
</mapper>